# 英語学習アプリ v2.0 — 使用方法

## ファイル構成
```
index.html      ← メインアプリ（これ1つで動く）
manifest.json   ← PWA設定（ホーム画面追加用）
sw.js           ← オフライン対応
```

---

## 基本的な使い方

### ホーム画面
- **「記録＋開く」** を押す → タイマーが自動スタート＆教材を開く
- **「停止・記録」** を押す → 経過時間を自動計算して記録
- 記録はブラウザのlocalStorageに保存される

### 音声プレイヤー
1. 「MP3 / WAV ファイルを選択」でファイルを開く
2. 再生・一時停止は ▶ ボタン
3. A点・B点をセット → 「A-Bリピート」でループ再生
4. 速度変更（0.5×〜1.5×）
5. 「シャドーイング完了として記録」で記録

### ダッシュボード
- 連続日数・今日の学習時間・累計時間を表示
- 学習カレンダー（ヒートマップ）で継続を可視化
- 項目別の今日の進捗バー

### 手動記録
- 通勤中のスマホ学習など、後から入力する場合
- ＋ボタンで回数を追加
- 時間（分）を入力
- 「まとめて記録する」で保存

### 設定
- 各項目の教材URLと目標時間（分/日）を変更できる
- URLに `__player__` と入力するとアプリ内プレイヤーを開く
- 「設定を保存」で即時反映

---

## データ管理

### バックアップ（エクスポート）
1. 設定 → データ管理 → 「📤 エクスポート」
2. JSONファイルがダウンロードされる
3. 定期的にバックアップを推奨

### 別のブラウザ・端末への移行（インポート）
1. 元の端末でエクスポート
2. 新しい端末でアプリを開く
3. 設定 → データ管理 → 「📥 インポート」
4. エクスポートしたJSONを選択

### スマホとのデータ共有
1. PCでエクスポート
2. スマホにAirDropまたはメールで送る
3. スマホのアプリでインポート

---

## GitHub Pages での公開方法

1. GitHubで新しいリポジトリを作成（Public）
2. `index.html` `manifest.json` `sw.js` をアップロード
3. Settings → Pages → Branch: main / (root) → Save
4. `https://ユーザー名.github.io/リポジトリ名/` で公開

---

## iPhoneのホーム画面に追加（PWA）

1. SafariでアプリのURLを開く
2. 下部の共有ボタン（□↑）をタップ
3. 「ホーム画面に追加」をタップ
4. 「追加」をタップ

ホーム画面から起動するとSafariのUIが消えてフルスクリーンになり、
下部のタブバーが正しく表示されます。

---

## 注意事項

- データはブラウザのlocalStorageに保存されます
- ブラウザのデータを消去するとデータも消えます → 定期的にエクスポートを推奨
- PCとスマホは自動同期されません → エクスポート/インポートで手動移行
- シークレットモードではデータが保存されません
